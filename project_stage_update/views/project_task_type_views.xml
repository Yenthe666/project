<?xml version="1.0"?>
<odoo>
    <record id="project_task_type_project_stage_form" model="ir.ui.view">
        <field name="name">project.task.type.form.inherit</field>
        <field name="model">project.task.type</field>
        <field name="inherit_id" ref="project.task_type_edit"/>
        <field name="arch" type="xml">
            <field name="project_ids" position="after">
                <field name="is_finished" widget="boolean_toggle"/>
            </field>
        </field>
    </record>

</odoo>